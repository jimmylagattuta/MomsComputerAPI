{
  "name": "moms-computer-portal-build",
  "description": "Build scripts for Rails + React portal on Heroku",
  "engines": {
    "node": "18.17.0"
  },
  "scripts": {
    "clean": "rm -rf public/portal",
    "build": "npm install --prefix client && npm run build --prefix client",
    "deploy": "mkdir -p public/portal && cp -a client/build/. public/portal/",
    "heroku-postbuild": "npm run clean && npm run build && npm run deploy"
  }
}